# Core FastAPI and ASGI server
fastapi>=0.104.0
uvicorn>=0.24.0  # Use minimal version (not [standard]) for smaller size
pydantic>=2.5.0
pydantic-settings>=2.1.0

# LiveKit dependencies
# Note: livekit-agents may have heavy optional dependencies
# Use only if needed, or consider API-based alternatives
livekit>=0.11.0
livekit-agents>=0.9.0

# Environment management
python-dotenv>=1.0.0

# Async HTTP client (optional, for external API calls)
httpx>=0.25.0

# Logging (removed structlog for smaller bundle size)
# Use Python stdlib logging instead

