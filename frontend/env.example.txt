# Backend API URL
# Copy this file to .env.local for local development
# For Vercel, add this as an environment variable named VITE_BACKEND_URL

VITE_BACKEND_URL=https://ai-voice-agent-backend.onrender.com

# Note: Vite uses the VITE_ prefix for client-side environment variables
# In Vercel, add this as an environment variable named VITE_BACKEND_URL

