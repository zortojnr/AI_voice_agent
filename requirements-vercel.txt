# Optimized requirements for Vercel deployment
# This version minimizes dependencies to stay under 250 MB limit

# Core FastAPI and ASGI server (minimal)
fastapi==0.115.0
uvicorn==0.34.0  # Minimal version without [standard] extras
pydantic==2.9.2
pydantic-settings==2.6.0

# LiveKit dependencies (core only - no ML model dependencies)
livekit==0.11.2
# Note: livekit-agents may pull heavy deps - consider API-based approach
livekit-agents==0.10.1

# Environment management
python-dotenv==1.0.1

# Async HTTP client (lightweight)
httpx==0.27.2

# Removed heavy optional dependencies:
# - structlog (logging can use stdlib)
# - uvicorn[standard] extras (includes watchfiles, websockets which add size)

